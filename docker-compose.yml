nginx:
  build: nginx/
  links:
    - php:php
  ports:
   - "80:80"
  volumes:
   - data/www:/var/www
redis:
  build: redis/
  expose:
   - "6379"
  volumes:
   - data/redis:/var/lib/redis
php:
  build: php/
  expose:
   - "9000"
  links:
   - redis:redis
  volumes:
   - data/www:/var/www
